// 20. Напишите код, который преобразовывает и объединяет все
// элементы массива в одно строковое значение. Элементы массива 
// будут разделены запятой.

let arr = [5, 8, 'test', 7, 'b', 789, 'fff']

qwe = (mass) => {
    return mass.join('')
}

console.log(qwe(arr));
